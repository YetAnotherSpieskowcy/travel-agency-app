name: Selenium tests

on:
  push:
    branches:
      - main
  pull_request:
    branches: main
permissions: read-all
jobs:
  build:
    name: Selenium tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0